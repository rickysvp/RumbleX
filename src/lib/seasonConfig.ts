/**
 * Season configuration for RumbleX.
 * All components must read from this config to ensure consistency.
 */
export const SEASON_CONFIG = {
  /** Minimum kills to qualify for the season airdrop */
  SEASON_KILL_THRESHOLD: 100,
  
  /** Percentage of each round's entry pool that feeds the season prize pool */
  SEASON_POOL_CUT: 0.10,
  
  /** Length of each season in days */
  SEASON_DURATION_DAYS: 14,
};
